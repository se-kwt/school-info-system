DATABASE_URL="postgresql://school_is:school_is@localhost:5432/school_is"
JWT_SECRET="change-me"
# Set to "true" only in local/dev environments to have send-otp return the plaintext
# code so the login page can show it as a toast. Never set this in a real deployment.
EXPOSE_OTP_FOR_TESTING="false"
